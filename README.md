# clean-recyclebin-dirs
This script will check and delete every $RECYCLE.BIN folder in $dirs array

Created this script to clean out $RECYCLE.BIN dirs that is created everytime I run Windows in VirtualBox.
